<h1>Listing posts</h1>

<table>
  <tr>
    <th>Description</th>
    <th>Rigtig svar</th>
    <th>Rigtig svar ans</th>
    <th>Forkert svar</th>
    <th>Try again</th>
    <th>Placering</th>
    <th>Løb</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @posts.each do |post| %>
  <tr>
    <td><%= post.description %></td>
    <td><%= post.rigtig_svar %></td>
    <td><%= post.rigtig_svar_ans %></td>
    <td><%= post.forkert_svar %></td>
    <td><%= post.try_again %></td>
	<td><%= post.placement %></td>
    <td><%= post.loeb.name unless post.loeb.nil? %></td>
    <td><%= link_to 'Show', post %></td>
    <td><%= link_to 'Edit', edit_post_path(post) %></td>
    <td><%= link_to 'Destroy', post, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Post', new_post_path %>
